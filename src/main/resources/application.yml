server:
  port: 8082
spring:
  application:
    name: product-service
  config:
    import: "optional:configserver:"
  data:
    mongodb:
      host: "localhost"
      port: "27017"
      username: user
      password: password
      database: profiles